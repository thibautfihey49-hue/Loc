# Loc - Tracker Invisible

## 📦 Récupérer les APKs
Va sur **Actions** → clique sur **Build APK Debug** → en bas dans **Artifacts**, télécharge les APKs.

## ⚙️ Configuration
- Modifie `PARENT_NUM` dans `MapActivity.kt` avec ton numéro
- Pousse sur main → build démarre tout seul
